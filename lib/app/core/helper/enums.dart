enum EnGender { male, female }

enum EnUserType { admin, member, scholar }

enum EnHadithType { marfu, mawquf, qudsi, atharSahaba }

enum EnSearchSource { hadith, fakeHadith }

enum EnProUpStatus { pendingDocuments, underReview, approved, rejected }

//enum ActitviyType {  }
